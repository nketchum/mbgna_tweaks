{% set classes = "office-hours-status-text office-hours-status-text--" ~ (is_open ? "open" : "closed") %}
<div{{ attributes.addClass(classes) }}>
  {% if is_open %}
    {{ open_text }}
  {% else %}
    {{ closed_text }}
  {% endif %}
</div>
